	最早：select 生成多个候选框，再经过CNN对候选框的图像进行特征提取，最后再用SVM进行分类；
	RCNN：select生成多个候选框，用CNN提取整副图像的特征，选择候选框对应的特征，再通过SVM进行分类；
	Fast RCNN：select生成多个候选框，用CNN提取整张图像的特征，选择候选框对应的特征，由于后续要用到全连接进行分类和回归，所以使用ROI pooling方法，无论候选框对应的特征图多大，都将其按照固定规则切割，再进行max pooling，从而保证ROI pooling后的结果都是一样大小，从而支持全连接；
	Faster RCNN：前面都需要用传统的算法进行候选框的生成，这里采用RPN网络生成各样的anchor，来替代这种传统的候选框的生成；
	YOLO
